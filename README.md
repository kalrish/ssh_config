# OpenSSH client configuration

This repository contains my personal OpenSSH client configuration, meant to be located in `~/.ssh/`.

Configuration meant to be kept secret (like that for work) lives in separate, private repositories and is imported as submodules.

For information about the configuration file structure and the possible settings, refer to [`ssh_config(5)`](https://man.archlinux.org/man/ssh_config.5).
